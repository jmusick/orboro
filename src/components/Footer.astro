---
import Icon from "./Icon.astro";
const year = new Date().getFullYear();
const version = "1.7.0";
---

<footer class="site-footer">
  <div class="footer-inner">
    <span class="copyright">&copy; {year} Orboro.net. All rights reserved.</span>
    <span class="version">v{version}</span>
    <nav class="footer-nav">
      <a href="/admin"><Icon name="settings" size={13} />Admin</a>
      <a href="https://github.com/jmusick/orboro" target="_blank" rel="noopener"><Icon name="github" size={13} filled />Source</a>
      <a href="/privacy-policy"><Icon name="shield" size={13} />Privacy Policy</a>
    </nav>
  </div>
</footer>

<style>
  .site-footer {
    color: var(--muted);
    font-size: 0.85rem;
    margin-top: 2rem;
    padding-bottom: 2rem;
  }

  .footer-inner {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    gap: 1rem;
  }

  .copyright {
    justify-self: start;
  }

  .footer-nav {
    display: flex;
    gap: 1.25rem;
    justify-self: end;
  }

  .footer-nav a {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    color: var(--muted);
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .footer-nav a:hover {
    color: var(--accent);
  }

  .version {
    justify-self: center;
    opacity: 0.5;
    font-size: 0.8rem;
  }

  @media (max-width: 600px) {
    .footer-inner {
      grid-template-columns: 1fr;
      align-items: center;
      text-align: center;
    }

    .copyright,
    .version,
    .footer-nav {
      justify-self: center;
    }
  }
</style>
